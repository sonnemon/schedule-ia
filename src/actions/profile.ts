import { User } from "next-auth";

export async function getProfile(user: User) {
  // const allUsers = await db.query.users.findFirst();
  // const user2 = await db.select(users).where({ email: user.email }).run();
}
